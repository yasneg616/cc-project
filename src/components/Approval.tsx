import {ShieldAlert} from 'lucide-react';
export default function Approval({event,onClose}:{event:AgentEvent;onClose:(answer:string)=>void}){return <div className="modal-backdrop"><div className="approval"><div className="approval-icon"><ShieldAlert/></div><h2>Claude 请求权限</h2><p>{event.content}</p><div className="approval-meta"><span>操作来自 Claude Code CLI</span><span>请确认你信任此操作</span></div><div className="approval-buttons"><button onClick={()=>onClose('n')}>拒绝</button><button onClick={()=>onClose('y')}>本次允许</button><button className="primary" onClick={()=>onClose('a')}>总是允许</button></div></div></div>}
